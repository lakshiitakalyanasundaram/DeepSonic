{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "build": {
    "installCommand": "echo 'Skipping pip install'",
    "buildCommand": "python app.py"
  }
}
